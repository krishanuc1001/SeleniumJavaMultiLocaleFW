## Selenium Java Multi Locale Framework

### For running tests with French language

```zsh
mvn clean install -Dlang=french
```

### For running tests with English language

```zsh
mvn clean install -Dlang=english 
```